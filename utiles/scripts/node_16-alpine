node:16-alpine3.16

// pwd es el path donde se encuentra la terminal en el momento de llamar el script
-> "$(pwd)" en linux 
--> ${PWD} en powershell
-> %cd% en cmd

// sh es un commando shell para que una vez se ejecute, lance los comandos que se le pasan como string

docker container run `
--name nest-app `
-w /app `
-p 3001:3000 `
-v  ${PWD}:/app `
node:16-alpine3.16 `
sh -c "yarn install && yarn start:dev"


// Para hacerlo desde la distro de ubuntu en wl WSL2

!!!!! SEGUIR LOS PASOS DEL PROBLEMA EN WINDOWS Y APLICARLO DESDE EL WSL

docker container run \
--name nest-app \
-w /app \
-p 3002:3000 \
-v $(pwd):/app \
node:16-alpine3.16 \
sh -c "yarn install && yarn start:dev"